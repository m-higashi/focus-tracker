# 集中トラッカー 説明書

(2026年8月1日版)

PCでの作業を「1件終わるごとにワンタップ」で記録し、休憩のタイミングを知らせてくれて、睡眠時間と集中力の統計まで見られるアプリです。画面にはキャラクターが常駐していて、時間帯で背景が変わります。

---

## 1. 起動とアクセス

> **この説明書は Windows の書き方(`Start.bat` や `data\focus.db`)で説明しています。**
> Mac をお使いの場合は、下の「Mac をお使いのかたへ」の読み替え表をご覧ください。

### はじめて使うPC・引っ越しのとき
**`インストールガイド.md` / `.txt`** を見てください。Windows では `Setup.bat` が環境チェックを、
`RegisterAutostart.cmd` がPC起動時の自動開始の登録をやってくれます
(**Mac にはこの2つはありません**。Node.js を自分で入れる手順と、ログイン項目への登録が代わりになります)。

### 起動する
- **Windows**: フォルダ内の **`Start.bat` をダブルクリック**。黒い窓が1つ開きます
- **Mac / Linux**: フォルダ内の **`Start.command` をダブルクリック**。ターミナルの窓が1つ開きます

開いている間はアプリが動いています(閉じるとアプリも止まります)。

### Mac をお使いのかたへ(読み替え表)

| この説明書の書き方 | Mac では |
|---|---|
| `Start.bat` | `Start.command` |
| `backup.cmd` | `backup.command` |
| `Setup.bat` | ありません(Node.js をご自分で入れます) |
| `RegisterAutostart.cmd` / `UnregisterAutostart.cmd` | ありません(「システム設定」→「一般」→「ログイン項目」で登録・解除します) |
| `data\focus.db` | `data/focus.db` |
| `backups\` | `backups/` |
| 黒い窓 | ターミナルの窓 |

初回だけ「開発元を確認できません」と出ることがあります。対処は `インストールガイド` の
「1-M. Macに入れる」をご覧ください。

### 開く
- このPCから: `http://127.0.0.1:3000`(起動時の窓に表示されるものと同じです)
- スマホ・他のPCから: 起動時の黒い窓に表示される `http://100.x.x.x:3000` のアドレス
  (Tailscaleに接続している端末からのみアクセスできます。社内LANには公開されていません)

### データの保存場所と引越し
すべて `data\focus.db` という1つのファイルに入っています。アプリ一式はこのフォルダで完結しているので、
**アプリを止めてからフォルダごとコピーすれば、バックアップも別PCへの引越しも完了**です。

**アプリを動かしたまま移したいとき**は、下のバックアップで作った `backups\focus-日時.db` を**1つだけ**
持っていき、移した先の `backups\` に置いて「復元」してください(**WindowsとMacの間でも同じ手順で移せます。
実機で確認済みです**)。

> ⚠️`data\focus.db` だけを**動作中に**コピーするのは避けてください。書きかけの記録が別ファイル
> (`focus.db-wal`)にあるため、直前の記録が欠けることがあります。上の2つの方法ならこの心配はありません。

### バックアップ
**毎日、その日最初の「🏢 勤務開始」を押した瞬間に自動でバックアップされます**(何もしなくてOK)。手動で取りたいときは設定タブの「💾 バックアップ作成」ボタンでいつでも作れます。`backups\` フォルダに日時付きで保存され、**新しい30個だけ残して古いものは自動で消えます**。「🩺 データを点検」ボタンでデータの健康状態も確認できます。

アプリを開かずに取りたいときは **`backup.cmd`(Mac は `backup.command`)をダブルクリック**しても構いません。
ボタンと**まったく同じもの**が作られ、同じように「復元」から戻せます。

### バックアップの復元
設定タブの「データ管理」で、戻したい日時のバックアップを選んで「⏪ このバックアップに復元」を押します。押し間違い防止のため、**5秒以内にもう一度押すと実行**されます。

- 復元すると記録・イベント・設定がすべて選んだ時点の内容に戻ります
- 復元の直前の状態も自動でバックアップされるので、「やっぱり戻す前が良かった」というときは、その自動バックアップ(復元した時刻のもの)を選んでもう一度復元すればやり直せます
- アプリを起動したまま操作でき、完了すると画面が自動で最新になります

### スマホのホーム画面にアプリとして追加
- **iPhone**: Safariで開く → 共有ボタン → 「ホーム画面に追加」。以降はアイコンから全画面アプリとして起動できます。**Googleアプリ内のブラウザからは追加できない**ので、一度Safariで開いてください(URLは同じ)
- **Android**: Chromeで開く → メニュー → 「ホーム画面に追加」

---

## 2. 毎日の基本的な使い方(メイン画面)

| タイミング | 操作 |
|---|---|
| 朝 | 左側の「🏢 勤務開始」を押す → キャラがストレッチ |
| 作業が1件終わるたび | 下の「易」「普通」「難」のどれかをタップ(これだけ) |
| 短い休憩 | 「☕ 休憩」で離席 → 戻ったら「▶ 再開」 |
| 昼休み | 「🏠 勤務終了」→ 昼休み →「🏢 勤務開始」 |
| 夜 | 「🏠 勤務終了」→ キャラが「おつかれさま」+**リザルト画面**(件数・実働・集中度・自己ベスト更新) |
| 寝るとき | 「🛏 就寝」を押す → キャラもおやすみモードに |
| 朝起きたら | 「🌅 起床」を押す(前夜からの**睡眠時間が記録**され、統計で集中力との関係が見られます) |

- 終了時刻と所要時間は自動で記録されます。休憩中・勤務外の時間は所要時間に含まれません
- **深夜0時をまたいで働いても大丈夫**: 「🏠 勤務終了」を押すまでは前日の勤務の続きとして扱われ、0時以降の記録・実働・リザルトも勤務を開始した日に数えられます(日付が切り替わるのは退勤したとき)
- **押し間違えたら**: 右下の「↩ 取消」で直前の記録を1タップで取り消し
- **記録を消したいとき**: ゴミ箱ボタン→「削除?」に変わる→3秒以内にもう一度押すと削除(誤タップ防止)
- **ダークモード**: PC・スマホがダークモードならアプリも自動でダーク配色になります

- **就寝・起床ボタンはどこ?**: 勤務時間外は「☕ 休憩」「▶ 再開」ボタンが自動で「🛏 就寝」「🌅 起床」に切り替わります(勤務中は休憩ボタンに戻ります)
- **🚙 移動ボタン**: 勤務時間外は「🏠 勤務終了」が「🚙 移動」に変わり、押すたびに背景が平日版⇄休日版で切り替わります
- **夜ふかししていると**、休憩アラートと同じ位置にポップアップで就寝を促します(「🛏 就寝する」でそのまま記録、「1時間後に再通知」でスヌーズ、「✕」でその夜はもう出ません)。出し始める時刻は設定タブ「就寝アラート」で22時〜2時から選べます(「通知しない」も可。既定は0時)

### 画面の見方
- 上部バッジ: いまの状態(勤務中/休憩中/勤務外/睡眠中)。サーバーと通信できないときは「接続なし」表示になります(PCの黒い窓とTailscaleを確認)
- 上部チップ: 本日の件数/実働時間/連続作業時間/平均所要時間/**集中度**(1.0が普段どおり。緑=好調、オレンジ=低調)
- 時間の表記: **時:分は「3:32」、分:秒は「5'02」**と記号で区別しています(実働・連続は時:分、所要時間は分:秒)
- 「連続」チップは**タップで「休憩まで あと◯分」のカウントダウン表示**に切替可能
- **押し忘れ検知**: 勤務が日をまたいだまま2時間以上動きがないとき(=前日の勤務終了の押し忘れ)や、勤務中の長時間無記録(設定変更可)のとき、青い確認バーが出てワンタップで修正できます。深夜0時をまたいで働き続けている間は出ません
- 下部ゲージ: 疲労度(たまるほど右へ。しきい値でアラート)
- 本日の記録: ステージ下の「📋」をタップで開閉。難易度・メモの修正、削除もここから

---

## 3. 休憩アラート

次のどちらかで「QUEST」ポップアップ+キャラのストレッチでお知らせします。

1. **連続作業が50分**を超えたとき
2. **疲労度スコアがしきい値(既定15)** を超えたとき
   (易+1 / 普通+2 / 難+4、さらに連続10分ごとに+1。「難」が続く日は早めに休憩を促します)

難易度は設定タブで追加・変更できます。ボタンの色は疲労度加算の軽い順に緑→青→赤のグラデーションから自動で割り当てられるので、段階を増やしても色の意味(緑=軽い、赤=重い)は変わりません。

アラートが出たら:
- 「☕ 休憩開始」… 休憩に入る(カウンタとスコアはリセット)
- 「5分後に再通知」… いまは手が離せないとき(スヌーズ)
- 「✕」… 今回は無視する

数値はすべて設定タブで変更できます。**設定は変更した瞬間に自動で保存されます**(保存ボタンはありません)。

---

## 4. 記録タブ(押し忘れ・修正)

過去の日付を選んで、作業記録・勤務/休憩/就寝/起床イベントの追加・修正・削除ができます。**作業記録とイベントは分けずに1本の時系列リスト**で表示されるので、1日の流れがそのまま読めます(追加は一番下の2行: 上=作業記録、下=イベント)。作業記録・イベントのどちらにもメモを付けられます(イベントのメモは打刻CSVにも出ます)。日付の横の「◀」「▶」で前の日・次の日へすぐ移動できます(押し忘れ修正は「昨日」を開くことが多いので便利です)。勤務開始を押し忘れた日も、正しい時刻にイベントを追加すれば所要時間は全件自動で再計算されます。就寝・起床の押し忘れもここで直せます(就寝が14時間を超えると起床の押し忘れ確認バーも出ます)。

---

## 5. 統計タブ

**集中度指数** = 難易度ごとの「自分の普段の所要時間(中央値)」÷ 実際の時間。1.0が普段どおり、1.2は好調、0.8は低調。データが5件たまるまでは「参考値」表示です。

- 日(本日): 件数、**本日の集中度**(その日の全記録の平均)、実働、平均所要、難易度内訳、**昨夜の睡眠**、集中度の推移グラフ(横軸は時刻。点の色=難易度、紫の縦線=休憩した位置で、グラフ右上の☕ボタンで表示/非表示を切り替えられます(非表示中は☕がグレーになります)。移動平均の線は昼休みなど長い中断で区切られ、勤務開始・休憩明けの時点から始まります)
- 週/月: 日別件数、難易度別の平均所要時間の推移、日別の集中度、連続作業時間と集中度(紫の縦線=アラート設定値。線より右で集中度が下がっていればアラートを早めるサイン)、休憩回数と集中度の関係(1点=1回の勤務。勤務中に取った休憩の回数×その勤務の集中度。半日勤務や夜の勤務もそれぞれ別の点になります)、時間帯×曜日の集中度ヒートマップ(赤=低調、青=普段どおり、緑=好調)
- **睡眠の統計**(週/月):
  - **睡眠の記録(平均睡眠 ○時間○分)**: 見出しにその期間の平均睡眠時間を表示。就寝〜起床の時間帯を1日1本の縦帯で表示。帯の色はその日の集中度(緑=好調、青=普段どおり、赤=低調)。夜ふかしの翌日に赤へ寄っていないかが一目でわかります
  - **睡眠時間と集中度の関係**: 前夜の睡眠時間とその日の集中度の散布図。自分に合う睡眠時間の目安が見えてきます
  - 睡眠は「起床した日」に数えます(1/15の夜に寝て1/16の朝に起きた分は、1/16の睡眠)
- **年**: 直近12ヶ月を月単位で表示 — 月別件数(難易度の積み上げ)、難易度別の平均所要時間(月平均)、月別の集中度、時間帯×曜日ヒートマップ、月別の平均睡眠時間、睡眠時間と集中度の関係
- **過去にさかのぼる**: 期間ボタンの下の「◀」「▶」で前の日・前の週・前の月・前の年へ移動できます(「▶」で今に戻る。ラベルに表示中の期間が出ます)
- **CSVダウンロード**(週/月/年画面の一番下): 「⬇ 記録CSV」で全作業記録、「⬇ 打刻CSV」で勤務・休憩・就寝・起床の全イベント(Excel等で開けます)

---

## 6. キャラクターについて

### いま入っているキャラ
`assets\character\` に白衣を着た動物のお医者さんが5種入っています: **cat(ねこ)/ dog(いぬ)/ bird(とり)/ frog(かえる)/ rabbit(うさぎ)**

各キャラ4差分: `idle.png`(通常)/ `happy.png`(記録時・勤務終了時・就寝時)/ `stretch.png`(アラート時・勤務開始時・起床時)/ `rest.png`(休憩中・睡眠中)

キャラはゆっくり呼吸するように動きます(OSの「視差効果を減らす」設定がONだと止まります)。

### 切り替え・追加
- 設定タブ →「キャラクターセット」で**選ぶだけで即反映**(保存ボタン不要)。「🎲 日替わりランダム」で毎日ちがうキャラに
- 増やすには `assets\character\` の下に新フォルダを作って4つの画像を入れるだけ(リロードで反映)
- `happy_01.png`, `happy_02.png` のように置くとランダム表示。どんなサイズでも動きます(自動で拡大縮小)
- 生成AIで新キャラを作るときは、同梱の **`キャラ作成プロンプト.txt`** を使うと4差分シートが作れます(参考画像を添付して貼るだけ)

### 画像を新しく作るときの推奨

| 項目 | 推奨 |
|---|---|
| サイズ | **縦800〜1200px(1000px前後)**。横は縦と同じまで、縦長シルエット推奨 |
| 形式 | 背景透過のPNG(またはWebP) |
| 顔の位置 | **画像の上から1/4〜1/3、左右中央**(右端の数字チップ・左上のバッジ・下側のQUESTパネルと重なりません) |
| 足元 | 画像の**下端ぴったり**に(下に透明余白があると宙に浮いて見える) |
| 4差分の統一 | idle/happy/stretch/rest は**同じ縮尺・同じ足元ライン**で(バラバラだと切り替え時にガタつく) |

---

## 7. 背景画像について

`assets\bg\` に時間帯別4枚を**平日版・休日版**の2セット収録。時刻で自動切替されます。

| 時間帯 | 平日版 | 休日版 |
|---|---|---|
| 朝(4時〜10時) | morning.png | morning_holiday.png |
| 昼(10時〜16時) | day.png | day_holiday.png |
| 夕(16時〜19時) | evening.png | evening_holiday.png |
| 夜(19時〜4時) | night.png | night_holiday.png |

- **平日版⇄休日版の切り替え**: 勤務外のとき「🏠 勤務終了」ボタンが「🚙 移動」に変わり、押すたびに切り替わります。設定タブ「ステージ背景」の「🕐 時間帯で自動・平日」「🚙 時間帯で自動・休日」でも選べます
- 差し替えは同名ファイルの上書きだけ(リロードで反映)
- **季節イベント用の固定背景**: `assets\bg\` に好きな名前で画像を置く(例: `xmas.png`)と設定タブ「ステージ背景」の選択肢に自動で登場。選ぶだけで即反映(時間帯自動は停止)。「🕐 時間帯で自動・平日」で元に戻る
- 新しく作るなら **縦長2:3(例: 900×1350px)** 推奨。下(床)基準で表示され、端末により上と左右が切れるので、見せたいものは中央〜下寄りに

---

## 8. 困ったとき

- **スマホからつながらない**: スマホのTailscale接続と、PCでアプリが起動しているか(黒い窓)を確認。PC再起動後は Start.bat をもう一度
- **所要時間が「—」**: 勤務開始の押し忘れ。記録タブでイベントを追加すれば再計算されます
- **データがおかしい気がする**: 設定タブ「🩺 データを点検」→「✅ 整合性OK」なら問題なし
- **データを戻したい**: 設定タブの「データ管理」から復元できます(「バックアップの復元」参照)。アプリが起動できないほど壊れている場合のみ手動で: `backups\` のファイルを `data\focus.db` に上書きコピーし、`focus.db-wal` と `focus.db-shm` を削除して再起動
