# 集中トラッカー インストールガイド

新しいPCへの導入・引っ越し・アンインストールの手順です。

---

## 0. アプリを手に入れる(GitHub から)

すでにフォルダをお持ちなら、この節は飛ばして「1.」へ進んでください。

**かんたんな方法** — ソフトの追加は不要です。

| 手順 | やること |
|---|---|
| 1 | GitHub のページを開く → https://github.com/m-higashi/focus-tracker |
| 2 | ファイル一覧の**右上にある緑色の「Code」ボタン**を押す |
| 3 | メニューのいちばん下の **「Download ZIP」** を押す |
| 4 | ダウンロードした ZIP を展開する(フォルダ名は何でも構いません) |

**git を使う方法** — 更新を受け取りやすくなります。ターミナル(Macの場合)またはコマンドプロンプトで:

```
git clone https://github.com/m-higashi/focus-tracker.git
```

更新するときは、そのフォルダの中で `git pull` を実行するだけです。
**記録したデータ(`data/` の中)は更新の対象外なので消えません。**

> **Windows で Node.js を入れたくない場合**は、GitHub の「Releases」から
> **Node同梱zip**(`-win-x64.zip`)を選ぶと、解凍して `Start.bat` を押すだけで動きます。

---

## 1. はじめて使うPCに入れる(所要10分)

### Windows の場合

| 手順 | やること |
|---|---|
| 1 | **このフォルダをPCの好きな場所に置く**(どこでもOK、後から移動しても大丈夫) |
| 2 | **`Setup.bat` をダブルクリック** — Node.js(動作に必要な無料ソフト)を自動チェック |
| 3 | (スマホからも使う場合)**Tailscale** をPCとスマホに入れて同じアカウントでログイン → https://tailscale.com/download |
| 4 | **`Start.bat` をダブルクリック** — 黒い窓に表示されるアドレスをブラウザで開く |
| 5 | (おすすめ)**`RegisterAutostart.cmd`** でPC起動時の自動開始を登録 |

**Setup.bat で [NG] が出たら**: 自動で開くページから「**LTS**」版をダウンロードし、設定は変えず「次へ」でインストール → もう一度 Setup.bat で [OK] を確認。

### Mac の場合

| 手順 | やること |
|---|---|
| 1 | **このフォルダをMacの好きな場所に置く**(どこでもOK) |
| 2 | **Node.js を入れる** — https://nodejs.org/ から「**LTS**」版をダウンロードしてインストール(設定は変えず進めるだけ) |
| 3 | **実行できるようにする**(ZIPで入れた場合のみ) — 「ターミナル」を開き、`chmod +x ` と入力してから **`Start.command` と `backup.command` をターミナルの窓にドラッグ&ドロップ** → Enter。**`git clone` で入れた場合は実行権限が付いているので不要です** |
| 4 | (スマホからも使う場合)**Tailscale** をMacとスマホに入れて同じアカウントでログイン |
| 5 | **`Start.command` をダブルクリック** — ターミナルに表示されるアドレスをブラウザで開く |
| 6 | (おすすめ)「システム設定」→「一般」→「ログイン項目」で `Start.command` を追加すると、Mac起動時に自動で立ち上がります |

**「開発元を確認できないため開けません」「ブロックされました」と出たら**: 初回だけ許可の操作が要ります。
**macOS のバージョンで場所が違います。**

| バージョン | やること |
|---|---|
| **macOS 15 (Sequoia) 以降** | 「システム設定」→「**プライバシーとセキュリティ**」→ 下までスクロール →「"Start.command" は…ブロックされました」の右の **「このまま開く」** → 許可したらもう一度ダブルクリック |
| macOS 14 (Sonoma) 以前 | `Start.command` を**右クリック(control+クリック)→「開く」**→ 確認画面でもう一度「開く」 |
| どちらも面倒なとき | 「ターミナル」で `sh ` と打ち、半角スペースの後に `Start.command` をドラッグ&ドロップして Enter |

> Macでは Node.js のインストールが必要です(Windows向けの「Node同梱zip」はMacでは使えません)。

使い方は `説明書.md` / `説明書.txt` へ。

---

## 2. 別のPCへ引っ越す(データごと移す)

1. 今のPCでアプリの窓を閉じる(=停止)
2. **このフォルダを丸ごと新PCへコピー**(記録も設定も全部入り。これだけで引っ越し完了)
3. 新PCで起動 — Windows: `Setup.bat` → `Start.bat` / Mac: Node.js を入れて `chmod +x Start.command` → `Start.command`
4. 自動起動を使っていた場合: 旧PCで解除、新PCで登録し直す
   (Windows: `UnregisterAutostart.cmd` / `RegisterAutostart.cmd`、Mac: ログイン項目)

> **WindowsとMacの間で移してもそのまま使えます**(記録の中身は共通です)。

> スマホから使うアドレス(100.x.x.x)は新PCで変わります。起動時の黒い窓で新しいアドレスを確認してください。

---

## 3. アンインストール

1. `UnregisterAutostart.cmd` で自動起動を解除(登録していなければ不要)
2. フォルダを削除するだけ(レジストリ等は不使用。記録を残したい場合は先に `data\focus.db` をコピー)

---

## 4. うまくいかないとき

- **Setup.bat が何度も [NG]**: Node.js インストール後にPCを再起動してから再実行
- **Start.bat の窓がすぐ閉じる**: 窓のエラーメッセージを控えて相談
- **(Mac)ダブルクリックしても何も起きない**: ZIPで入れた場合は手順3の `chmod +x` を実行したか確認
- **(Mac)「開発元を確認できません」「ブロックされました」**: 初回だけ許可が要ります。
  **macOS 15以降**はシステム設定 →「プライバシーとセキュリティ」→「このまま開く」、
  **macOS 14以前**は右クリック →「開く」。上の「1. Mac の場合」の表もご覧ください
- **(Mac)「Node.js が見つかりません」と出る**: Node.js を入れ直すか、ターミナルで `node -v` が動くか確認
- **スマホからつながらない**: 両端末のTailscaleログイン確認/引っ越し後は旧アドレスを開いていないか確認
